const EthSDK=require('opensdk-javascript')


export class EthOpenSDK {
    constructor(apiClient){
        // read list of function in yaml
        // init instances of all function in EthSDK
        // const sdks=yaml.read(./eth.yaml)
        /* 
        sdks.forEach(SDK=>{

            this[SDK.name]=new SDK(apiClient)

        })
        
        
        */
    }
}