openapi: "3.0.2"
info:
  title: KLAY OPEN API
  version: "1.10.0"
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.html

servers:
  - url: http://localhost:7151

paths:
  /klay/gasPriceAt:
    post:
      operationId: gasPriceAt
      summary: "[gasPriceAt]"
      description: |
        Returns different values based on the condition described below. The unit of the return value is peb.
        
        - If baseFee is undefined in the header, it returns the unit price from the governance parameter
        
        - If the block is a pending block, it returns the gas price of the txpool.
        
        - Otherwise, it returns the base fee of the given block.
        
        **JSONRPC:** `klay_gasPriceAt`
      tags:
        - klay

      parameters:
        - name: blockNumber
          in: query
          description: Block number. If omitted, latest unit price will be returned.
          schema:
            oneOf:
              - $ref: "../../../components/schemas/common/Common.yaml#/components/schemas/IntegerBlockNumber"
              - $ref: "../../../components/schemas/common/Common.yaml#/components/schemas/HexadecimalBlockNumber"
      requestBody:
        content:
          application/json:
            schema:
              allOf:
                - $ref: "../../../components/requests/JsonRpcRequest.yaml#/components/schemas/JsonRpcRequest"
                - $ref: "#/components/schemas/KlayGasPriceAtReq"

      responses:
        200:
          description: Return integer of the current gas price in peb.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "../../../components/responses/JsonRpcResponse.yaml#/components/schemas/JsonRpcResponse"
                  - type: object
                    properties:
                      result:
                        type: string
                        description: "Integer of the current gas price in peb."
                        example: "0xAE9F7BCC00"

      externalDocs:
        url: https://docs.klaytn.foundation/content/dapp/json-rpc/api-references/klay/config#klay_gaspriceat
        description: Klaytn Docs
      x-codeSamples:
        - lang: "curl"
          label: "Curl"
          source:
            $ref: "../../../code-samples/curl/klay/configuration/gasPriceAt.sh"
        # - lang: "java"
        #   label: "Java"
        #   source:
        #     $ref: ""
        - lang: "javascript"
          label: "Javascript"
          source:
            $ref: "../../../code-samples/javascript/klay/configuration/gasPriceAt.js"
        - lang: "python"
          label: "Python"
          source:
            $ref: "../../../code-samples/python/klay/configuration/gasPriceAt.py"

components:
  schemas:
    KlayGasPriceAtReq:
      type: object
      required:
        - method
        - params
      properties:
        method:
          type: string
          default: 'klay_gasPriceAt'
        params:
          type: array
          items:
            anyOf:
              - $ref: "../../../components/schemas/common/Common.yaml#/components/schemas/IntegerBlockNumber"
              - $ref: "../../../components/schemas/common/Common.yaml#/components/schemas/HexadecimalBlockNumber"
          example:  ["0x64"]
