#!/bin/bash

CURRENT_FILE_DIR=$(cd "$( dirname "$_" )" && pwd)

java -ea                          \
  -Xms512M                        \
  -Xmx1024M                       \
  -server                         \
  -jar "${CURRENT_FILE_DIR}"/libs/web3rpc-openapi-generator-cli.jar "$@"
